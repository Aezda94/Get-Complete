# Field map — customer submission form

> PENDING. Built by diffing `examples/quotes/` against
> `examples/completed-forms/`. Until then this file is a placeholder and the
> skill must not guess.

For each field on the customer's form, this file will record:

| Form field | Source | Transform | Notes |
| --- | --- | --- | --- |
| _(field label as printed)_ | _(where it comes from in our quote)_ | _(format/rounding/case rule)_ | _(edge cases)_ |

Also captured here:
- Date format the customer expects
- Currency formatting and decimal places
- How multi-line-item quotes collapse onto the form
- Which fields are constants vs. per-quote
- Fields that are always left blank
